{
    "name": "korchid/calendar",
    "description": "Set security headers for Symfony apps through middleware",
    "type": "symfony-bundle",
    "require": {
        "php": ">=8.1",
        "symfony/framework-bundle": "^6.1"
    }
}